import React from 'react'

// Minimal placeholder for Sonner toaster used in app during development and
// to avoid adding a runtime dependency on the `sonner` package.
export const SonnerToaster: React.FC = () => null

export default SonnerToaster
