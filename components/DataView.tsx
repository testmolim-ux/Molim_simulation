import React from "react";

export const DataView: React.FC<any> = ({ onHome }) => {
  return (
    <div>
      <h2>Data View (stub)</h2>
      <button onClick={onHome}>Home</button>
    </div>
  );
};
